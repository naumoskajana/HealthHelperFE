import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#50C878',
    },
    secondary: {
      main: '#50C878',
    },
    background: {
      main: '#50C878',
    }
  },
});